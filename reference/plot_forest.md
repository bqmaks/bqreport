# Plot model estimates and confidence intervals

Plot model estimates and confidence intervals

## Usage

``` r
plot_forest(x, component = c("estimates", "contrasts"))
```

## Arguments

- x:

  An `analysis_result`.

- component:

  Either model `estimates` or `contrasts`.

## Value

A `ggplot` object.
