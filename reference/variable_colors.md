# Resolve colors registered for a categorical variable

Resolve colors registered for a categorical variable

## Usage

``` r
variable_colors(.data, .col)
```

## Arguments

- .data:

  A `bq_data` object.

- .col:

  Exactly one column selected with tidyselect.

## Value

A named character vector or `NULL`.
