# Render a bq table with gt

Render a bq table with gt

## Usage

``` r
as_gt(x, ...)
```

## Arguments

- x:

  A `bq_table`.

- ...:

  Additional arguments passed to
  [`gt::gt()`](https://gt.rstudio.com/reference/gt.html).

## Value

A `gt_tbl`.
