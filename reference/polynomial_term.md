# Construct a polynomial covariate term

Construct a polynomial covariate term

## Usage

``` r
polynomial_term(degree, raw = FALSE)
```

## Arguments

- degree:

  Positive polynomial degree.

- raw:

  Whether to use raw rather than orthogonal polynomials.

## Value

A `model_term_spec`.
