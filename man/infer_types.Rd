% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer-types.R
\name{infer_types}
\alias{infer_types}
\title{Infer analytic types for data columns}
\usage{
infer_types(data, variables = tidyselect::everything(), max_levels = 20L)
}
\arguments{
\item{data}{A \code{bq_data} object.}

\item{variables}{A tidyselect expression selecting one or more columns.
Defaults to all columns.}

\item{max_levels}{A positive whole number giving the largest number of
categories still read as \code{"nominal"}.}
}
\value{
\code{data} with inferred metadata added where type was missing.
}
\description{
Fills analytic metadata for selected columns whose type is still missing.
Existing type decisions are left untouched regardless of their source. The
data itself is never converted or otherwise modified.
}
\details{
Inference records its provenance in \code{type_source}. Binary events carry their
own source, distinguishing events dictated by a known coding from defaults
chosen by category order. An inferred ordinal order is expanded into the
separate flat level registry.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55), event = c(0, 1)))
data <- infer_types(data)
variables(data)
}
