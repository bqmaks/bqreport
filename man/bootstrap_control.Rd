% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-control.R
\name{bootstrap_control}
\alias{bootstrap_control}
\title{Configure bootstrap confidence intervals}
\usage{
bootstrap_control(
  method = "ordinary",
  iterations = 2000L,
  conf_type = "bca",
  seed = NULL,
  weight_type = NULL
)
}
\arguments{
\item{method}{Bootstrap method: \code{"ordinary"} for resampling observations or
\code{"fractional"} for fractional exponential weights.}

\item{iterations}{Number of bootstrap resamples. Must be one positive whole
number no greater than \code{.Machine$integer.max}.}

\item{conf_type}{Confidence-interval method. One of \code{"bca"}, \code{"percentile"}
and \code{"basic"}.}

\item{seed}{Integer seed used to reproduce the bootstrap resampling, or
\code{NULL} to use the current random-number stream. Must be a non-negative
whole number no greater than \code{.Machine$integer.max} when supplied.}

\item{weight_type}{Fractional-weight distribution. Must be \code{NULL} for an
ordinary bootstrap. For a fractional bootstrap, \code{NULL} selects
\code{"exponential"}, which is currently the only supported distribution.}
}
\value{
A \code{bq_bootstrap_control} specification.
}
\description{
Declares a reproducible nonparametric bootstrap configuration. The
configuration is computationally inert: bootstrap resampling is performed
only when an analytic function consumes it.
}
\details{
Ordinary bootstrap uses \code{\link[boot:boot]{boot::boot()}}. Fractional weighted bootstrap uses
\code{fwb::fwb()} and requires an analytic function that explicitly supports
weighted estimation. The selected engine is recorded in the specification.
}
\examples{
bootstrap_control()
bootstrap_control(method = "fractional", iterations = 5000, seed = 2026)
}
