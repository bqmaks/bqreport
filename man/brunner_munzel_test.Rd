% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brunner-munzel-test.R
\name{brunner_munzel_test}
\alias{brunner_munzel_test}
\title{Declare a Brunner-Munzel test}
\usage{
brunner_munzel_test(
  hypothesis = "two_sided",
  margin = NULL,
  benefit = NULL,
  inference = "asymptotic",
  permutation = NULL,
  bootstrap = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{hypothesis}{One of \code{"two_sided"}, \code{"equivalence"},
\code{"noninferiority"} and \code{"superiority"}.}

\item{margin}{Relevant deviation from the neutral relative effect \code{0.5}.
Must be \code{NULL} for a two-sided test, a positive scalar for equivalence or
noninferiority, a non-negative scalar for superiority, or named
\code{c(lower, upper)} deviations for asymmetric equivalence bounds.}

\item{benefit}{Beneficial outcome direction, \code{"higher"} or \code{"lower"}, for
noninferiority and superiority. Must otherwise be \code{NULL}.}

\item{inference}{\code{"asymptotic"} for the t approximation, \code{"logit"} for
logit-scale inference with a range-preserving interval, or \code{"permutation"}
for a studentized randomization test.}

\item{permutation}{A \code{\link[=permutation_control]{permutation_control()}} specification when
\code{inference = "permutation"}; otherwise \code{NULL}.}

\item{bootstrap}{\code{NULL} for the interval supplied by the test engine, or a
\code{\link[=bootstrap_control]{bootstrap_control()}} specification for a resampling interval around the
relative effect. Bootstrap does not change the test p-value.}

\item{conf_level}{Requested confidence level. Equivalence uses the
corresponding \code{2 * conf_level - 1} TOST interval.}
}
\value{
A \code{bq_brunner_munzel_test} analytic function.
}
\description{
Creates a terminal analytic function for an independent two-group
Brunner-Munzel test. The relative effect is oriented as the probability
that a comparison-group value exceeds a reference-group value, with half
credit for ties.
}
