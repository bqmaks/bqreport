% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dunn-test.R
\name{dunn_test}
\alias{dunn_test}
\title{Declare a Dunn multiple-comparison test}
\usage{
dunn_test(comparisons = "pairwise", reference = NULL, p_adjust = "holm")
}
\arguments{
\item{comparisons}{Comparison family: \code{"pairwise"}, \code{"reference"} or
\code{"consecutive"}.}

\item{reference}{Reference group value when \code{comparisons = "reference"};
otherwise \code{NULL}.}

\item{p_adjust}{Multiplicity method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} across the
selected family.}
}
\value{
A \code{bq_dunn_test} analytic function.
}
\description{
Creates a terminal analytic function for Dunn comparisons through
\code{PMCMRplus::kwAllPairsDunnTest()}. The engine first supplies unadjusted
all-pairs statistics; the declared family is then selected and its p-values
are adjusted together. This keeps pairwise, reference and consecutive
families distinct. No omnibus test is computed or returned.
}
\details{
The reported estimate is the difference in pooled mean ranks, oriented as
comparison minus reference. Dunn's procedure does not supply confidence
intervals, so interval fields are explicitly missing.
}
