% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-builder.R
\name{analysis_plan}
\alias{analysis_plan}
\title{Build an analysis plan incrementally}
\usage{
analysis_plan(.data)
}
\arguments{
\item{.data}{A \code{bq_data} object.}
}
\value{
An \code{analysis_plan_builder}.
}
\description{
\code{analysis_plan()} starts an accumulator tied to one \code{bq_data} object.
Analysis blocks can then be appended with \code{\link[=add_analysis]{add_analysis()}},
\code{\link[=add_descriptives]{add_descriptives()}}, and \code{\link[=add_correlations]{add_correlations()}}. \code{\link[=build_plan]{build_plan()}} returns the
accumulated plan without preflight, while calling \code{\link[=validate_plan]{validate_plan()}} on the
accumulator returns the ordinary validated \code{analysis_plan} tibble.
}
\details{
The builder retains the source data while tasks are being compiled. The
ordinary plan returned by \code{\link[=validate_plan]{validate_plan()}} remains separate from the data
and refers to variables through stable identifiers.
}
\examples{
data <- as_bq_data(tibble::tibble(
  y = c(1, 2, 3, 4),
  response = c(0, 1, 0, 1),
  treatment = factor(c("A", "A", "B", "B"))
)) |>
  set_outcome(y, type = "continuous") |>
  set_outcome(response, type = "binary", event = 1) |>
  set_predictor(treatment, type = "binary", reference = "A")
plan <- data |>
  analysis_plan() |>
  add_analysis(y, treatment) |>
  add_analysis(response, treatment) |>
  validate_plan()
}
