% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-plan.R
\name{validate_plan}
\alias{validate_plan}
\title{Validate an analysis plan against data}
\usage{
validate_plan(plan, data = NULL)
}
\arguments{
\item{plan}{An \code{analysis_plan}.}

\item{data}{A \code{bq_data} object. May be omitted when \code{plan} is an
\code{analysis_plan_builder}.}
}
\value{
A validated \code{analysis_plan}.
}
\description{
Preflight resolves variables by stable identifiers, refreshes formulas and
counts complete observations while treating labelled special missing values
as missing only in the internal analysis view.
}
\examples{
data <- as_bq_data(tibble::tibble(
  bmi = c(21.4, 27.9, 24.2, 30.1, 26.6, 23.0),
  treatment = factor(rep(c("Control", "Treatment"), 3))
)) |>
  set_outcome(bmi, type = "continuous") |>
  set_predictor(treatment, type = "binary", reference = "Control")
plan <- plan_analysis(data, bmi, treatment) |> validate_plan(data)
plan[, c("outcome", "predictor", "method", "status", "n_analyzed")]
}
