% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation-methods.R
\name{resampled_correlation}
\alias{resampled_correlation}
\title{Add bootstrap intervals and permutation inference to a correlation method}
\usage{
resampled_correlation(
  method = pearson_correlation(),
  bootstrap = 999L,
  permutations = 999L,
  seed,
  ci = c("percentile", "basic", "normal", "bca")
)
}
\arguments{
\item{method}{A correlation method specification.}

\item{bootstrap}{Number of bootstrap replicates, or zero.}

\item{permutations}{Number of permutation replicates, or zero.}

\item{seed}{Required integer seed used without changing global RNG state.}

\item{ci}{Bootstrap confidence interval computed by \code{\link[boot:boot.ci]{boot::boot.ci()}}.
Supported methods are \code{percentile}, \code{basic}, \code{normal}, and \code{bca}.}
}
\value{
A resampling \code{correlation_method_spec}.
}
\description{
Bootstrap replicates resample observations jointly with their analysis
weights. For subject-identified methods whole subjects are resampled
(cluster bootstrap) and permutation replicates permute outcome values
within subjects only, preserving the within-subject estimand.
}
\examples{
data <- as_bq_data(tibble::tibble(
  x = c(1, 4, 2, 8, 5, 9, 3, 7, 6, 10),
  y = c(2, 1, 5, 4, 8, 7, 3, 9, 6, 10)
))
method <- resampled_correlation(
  pearson_correlation(), bootstrap = 199, permutations = 199, seed = 42
)
result <- plan_correlations(data, x, with = y, method = method) |>
  validate_plan(data) |>
  run_analysis(data)
correlations(result)[, c("estimate", "conf_low", "conf_high", "p_value")]
}
