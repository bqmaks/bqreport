% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/t-family.R
\name{t_family}
\alias{t_family}
\title{Declare a family of parametric t-tests}
\usage{
t_family(
  comparisons = "pairwise",
  reference = NULL,
  var_equal = FALSE,
  effect_size = "none",
  p_adjust = "holm",
  conf_level = 0.95
)
}
\arguments{
\item{comparisons}{Comparison family: \code{"pairwise"} for every unordered pair,
\code{"reference"} for every other level versus one reference, or
\code{"consecutive"} for each level versus the preceding factor level.}

\item{reference}{Reference group value when \code{comparisons = "reference"};
otherwise \code{NULL}.}

\item{var_equal}{Whether every pair uses Student's equal-variance t-test.
\code{FALSE} uses Welch's t-test.}

\item{effect_size}{Effect size to report for every comparison. One of
\code{"none"}, \code{"cohens_d"} and \code{"hedges_g"}. Student's tests use a pooled
standard deviation and Welch's tests use an unpooled standard deviation.}

\item{p_adjust}{Multiplicity method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} across the
declared family. Defaults to \code{"holm"}.}

\item{conf_level}{Individual confidence level for every mean difference.}
}
\value{
A \code{bq_t_family} analytic function.
}
\description{
Creates a terminal analytic function for a declared family of two-sided
independent t-tests. P-values are adjusted together only across the
comparisons present in that family. No omnibus test is computed or returned.
}
\details{
Confidence intervals are the individual intervals returned by
\code{\link[stats:t.test]{stats::t.test()}}. They are deliberately marked as unadjusted because
general p-value procedures such as Holm or false-discovery-rate control do
not define matching simultaneous intervals.
}
\examples{
analysis <- t_family(
  comparisons = "reference",
  reference = "control",
  var_equal = FALSE,
  p_adjust = "holm"
)
}
