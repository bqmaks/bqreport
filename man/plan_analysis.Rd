% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-plan.R
\name{plan_analysis}
\alias{plan_analysis}
\title{Compile a univariable analysis plan}
\usage{
plan_analysis(
  .data,
  outcomes = all_outcomes(),
  predictors = all_predictors(),
  covariates = tidyselect::any_of(character()),
  weights = tidyselect::any_of(character()),
  cluster = tidyselect::any_of(character()),
  strata = tidyselect::any_of(character()),
  effect_modifiers = tidyselect::any_of(character()),
  variance = NULL,
  rules = NULL,
  confidence_level = 0.95
)
}
\arguments{
\item{.data}{A \code{bq_data} object.}

\item{outcomes}{Outcome columns selected with tidyselect.}

\item{predictors}{Predictor columns selected with tidyselect.}

\item{covariates}{Optional adjustment covariates selected with tidyselect.}

\item{weights}{Optional single configured weight column.}

\item{cluster}{Optional matched-set cluster column.}

\item{strata}{Optional columns defining independent analysis strata. Only
observed complete combinations are compiled.}

\item{effect_modifiers}{Optional variables interacting with the predictor.}

\item{variance}{Variance estimator. Defaults to \code{robust} for IPW and
\code{model_based} otherwise.}

\item{rules}{Optional concrete \code{analysis_rules}.}

\item{confidence_level}{Confidence level in the open interval \verb{(0, 1)}.}
}
\value{
An \code{analysis_plan} tibble.
}
\description{
\code{plan_analysis()} creates one inspectable task for each selected
outcome-predictor pair. The initial vertical slice supports continuous
outcomes with linear models and binary outcomes with logistic models.
}
\examples{
data <- as_bq_data(tibble::tibble(
  response = c(0, 0, 1, 0, 1, 1, 1, 0),
  treatment = factor(rep(c("Control", "Treatment"), each = 4)),
  age = c(44, 57, 51, 63, 46, 55, 60, 49)
)) |>
  set_outcome(response, type = "binary", event = 1) |>
  set_predictor(treatment, type = "binary", reference = "Control") |>
  set_predictor(age, type = "continuous")
plan <- plan_analysis(data, response, treatment, covariates = age)
plan[, c("analysis_id", "outcome", "predictor", "method", "status")]
}
