% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-presentation.R
\name{prepare_presentation}
\alias{prepare_presentation}
\title{Prepare analysis results for presentation}
\usage{
prepare_presentation(result)
}
\arguments{
\item{result}{A \code{bq_result} object.}
}
\value{
A \code{bq_presentation} object.
}
\description{
Compiles presentation decisions without formatting or changing computed
estimates. Enumerated values remain numeric so rounding, locale and text
layout can be applied later.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55, NA)))
data <- set_type(data, age, type_continuous())
average <- continuous_statistic(
  "average",
  function(x) {
    data.frame(
      mean = if (length(x) == 0L) NA_real_ else mean(x, na.rm = TRUE)
    )
  }
)
plan <- plan_summary(data) |>
  add_statistic(age, average)
plan <- add_display_rule(plan, age, enumerate_values())
prepare_presentation(run_analysis(plan))
}
