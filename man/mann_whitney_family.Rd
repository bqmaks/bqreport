% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mann-whitney-family.R
\name{mann_whitney_family}
\alias{mann_whitney_family}
\title{Declare a family of Mann-Whitney tests}
\usage{
mann_whitney_family(
  comparisons = "pairwise",
  reference = NULL,
  exact = "auto",
  continuity_correction = TRUE,
  p_adjust = "holm",
  conf_level = 0.95
)
}
\arguments{
\item{comparisons}{Comparison family: \code{"pairwise"}, \code{"reference"} or
\code{"consecutive"}.}

\item{reference}{Reference group value when \code{comparisons = "reference"};
otherwise \code{NULL}.}

\item{exact}{Exactness policy passed to \code{\link[=mann_whitney_test]{mann_whitney_test()}}: \code{"auto"},
\code{TRUE} or \code{FALSE}.}

\item{continuity_correction}{Whether asymptotic pairwise tests use a
continuity correction.}

\item{p_adjust}{Multiplicity method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} across the
declared family.}

\item{conf_level}{Individual Hodges-Lehmann confidence level.}
}
\value{
A \code{bq_mann_whitney_family} analytic function.
}
\description{
Creates a terminal analytic function for a declared family of two-sided
Mann-Whitney tests. Every pair is executed by \code{\link[=mann_whitney_test]{mann_whitney_test()}}, so
exactness, tie handling, continuity correction and the Hodges-Lehmann
estimand follow the same contract as the standalone two-group provider. No
omnibus test is computed or returned.
}
\details{
P-values are adjusted only across the comparisons in the declared family.
Hodges-Lehmann intervals remain individual unadjusted intervals and are
labelled accordingly.

Every comparison also supplies Cliff's delta, oriented as comparison minus
reference and computed directly from the Mann-Whitney statistic. The
standard \code{\link[stats:wilcox.test]{stats::wilcox.test()}} implementation does not supply a confidence
interval for Cliff's delta, so its effect-size interval fields remain
missing rather than introducing a second interval method.
}
