% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/t-test.R
\name{t_test}
\alias{t_test}
\title{Declare an independent two-sample t-test}
\usage{
t_test(
  var_equal = FALSE,
  hypothesis = "two_sided",
  margin = NULL,
  benefit = NULL,
  effect_size = "none",
  inference = "analytical",
  permutation = NULL,
  bootstrap = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{var_equal}{Whether to assume equal group variances. \code{FALSE} declares
Welch's two-sample t-test; \code{TRUE} declares Student's two-sample t-test.}

\item{hypothesis}{Hypothesis type. One of \code{"two_sided"}, \code{"equivalence"},
\code{"noninferiority"} and \code{"superiority"}.}

\item{margin}{Hypothesis margin in outcome units. Must be \code{NULL} for a
two-sided test, one positive number for noninferiority or equivalence, one
non-negative number for superiority, or a named \code{c(lower, upper)} vector
spanning zero for asymmetric equivalence bounds.}

\item{benefit}{Which outcome direction is beneficial for noninferiority and
superiority: \code{"higher"} or \code{"lower"}. Must be \code{NULL} for two-sided and
equivalence tests.}

\item{effect_size}{Effect size to report. One of \code{"none"}, \code{"cohens_d"}
and \code{"hedges_g"}. Student's test uses a pooled standard deviation and
Welch's test uses an unpooled standard deviation.}

\item{inference}{\code{"analytical"} for the usual t distribution or
\code{"permutation"} for a studentized randomization test.}

\item{permutation}{A \code{\link[=permutation_control]{permutation_control()}} specification when
\code{inference = "permutation"}; otherwise \code{NULL}.}

\item{bootstrap}{\code{NULL} for the test-engine interval or a
\code{\link[=bootstrap_control]{bootstrap_control()}} specification for ordinary or fractional weighted
bootstrap intervals around the mean difference and any requested effect
size.}

\item{conf_level}{Confidence level for the mean-difference interval. Must be
one finite number strictly between zero and one.}
}
\value{
A \code{bq_t_test} analytic function.
}
\description{
Creates an analytic function with its variance assumption and confidence
level fixed before analysis. The returned function is executed later by an
analysis plan with data and a compiled analysis context.
}
\details{
The analytic function supplies only the test result. It does not return a
fitted model and cannot supply within-group estimates or extractors.
}
\examples{
analysis <- t_test(
  var_equal = FALSE,
  hypothesis = "two_sided",
  effect_size = "none",
  conf_level = 0.95
)
analysis
}
