% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous-descriptives-extended.R
\name{continuous_descriptives_extended}
\alias{continuous_descriptives_extended}
\title{Declare extended continuous descriptive statistics}
\usage{
continuous_descriptives_extended()
}
\value{
A \code{bq_continuous_statistic} specification.
}
\description{
Creates a built-in continuous statistic containing the basic components
from \code{\link[=continuous_descriptives]{continuous_descriptives()}} followed by \code{iqr}, \code{mad}, \code{skewness} and
\code{excess_kurtosis}. Missing values are omitted before calculation.
}
\details{
IQR is the difference between the type-7 quartiles used by the basic set.
MAD uses the default consistency constant from \code{\link[stats:mad]{stats::mad()}}. Skewness and
excess kurtosis use the adjusted type-2 estimators from \code{datawizard}, which
correspond to the estimators commonly reported by SAS and SPSS. They return
missing values below three and four observations respectively, or when the
observed values have zero dispersion.

The two moment components are dimensionless and default to two decimal
places. This policy can be replaced with \code{\link[=set_component_rounding]{set_component_rounding()}}.
}
\examples{
if (requireNamespace("datawizard", quietly = TRUE)) {
  data <- as_bq_data(data.frame(age = c(40, 55, 61, NA)))
  data <- set_type(data, age, type_continuous())
  data <- set_rounding(data, age, 1)
  plan <- plan_summary(data) |>
    add_statistic(age, continuous_descriptives_extended())
  run_analysis(plan)
}
}
