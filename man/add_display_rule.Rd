% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-display-rule.R
\name{add_display_rule}
\alias{add_display_rule}
\title{Add a display rule to a summary plan}
\usage{
add_display_rule(plan, variables, rule)
}
\arguments{
\item{plan}{A \code{bq_plan_summary} object.}

\item{variables}{A tidyselect expression selecting one or more variables
already included in \code{plan$variables}.}

\item{rule}{A \code{bq_display_rule} specification such as one created by
\code{\link[=enumerate_values]{enumerate_values()}}.}
}
\value{
A copy of \code{plan} with the display rule registered and assigned.
}
\description{
Registers one display-rule specification and assigns it to selected summary
variables. The rule controls presentation only and does not change which
statistics are computed.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55, NA), bmi = c(22, 31, 27)))
plan <- plan_summary(data) |>
  add_statistic(c(age, bmi))
add_display_rule(plan, c(age, bmi), enumerate_values(max_n = 2L))
}
