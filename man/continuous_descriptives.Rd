% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous-descriptives.R
\name{continuous_descriptives}
\alias{continuous_descriptives}
\title{Declare basic continuous descriptive statistics}
\usage{
continuous_descriptives()
}
\value{
A \code{bq_continuous_statistic} specification.
}
\description{
Creates the built-in continuous statistic used by \code{\link[=add_statistic]{add_statistic()}} unless
another specification is supplied through its \code{statistic} argument.
Missing values are omitted before calculation. The returned components are
\code{mean}, \code{sd}, \code{median}, \code{q1}, \code{q3}, \code{min} and \code{max}; all use the source
variable's measurement scale and rounding policy.
}
\details{
Quartiles use \code{stats::quantile()} with \code{type = 7}. A cell without observed
values returns missing values for every component. A cell with one observed
value returns a missing standard deviation.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55, NA)))
data <- set_type(data, age, type_continuous())
data <- set_rounding(data, age, 1)
plan <- plan_summary(data) |>
  add_statistic(age)
run_analysis(plan)
}
