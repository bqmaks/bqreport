% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-summary-format.R
\name{set_summary_format}
\alias{set_summary_format}
\title{Set summary presentation formats}
\usage{
set_summary_format(data, variables, formats)
}
\arguments{
\item{data}{A \code{bq_data} object.}

\item{variables}{A tidyselect expression selecting one or more columns.}

\item{formats}{A non-empty character vector of presentation templates,
optionally with unique non-empty names.}
}
\value{
\code{data} with updated summary format metadata.
}
\description{
Records one or more ordered templates for presenting descriptive statistic
components of selected variables. Templates use component placeholders such
as \code{{mean}} and \code{{sd}}. Their existence is checked later against an analysis
plan, when the assigned statistics are known.
}
\details{
A completely unnamed \code{formats} vector is allowed. If names are supplied,
every format must have one unique, non-empty name. Names remain separate row
metadata and are not automatically added to the formatted value.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55)))
set_summary_format(
  data,
  age,
  c(
    "Mean (SD)" = "{mean} ({sd})",
    "Median (Q1; Q3)" = "{median} ({q1}; {q3})"
  )
)
}
