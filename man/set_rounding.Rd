% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-format.R
\name{set_rounding}
\alias{set_rounding}
\title{Set a rounding policy for variables}
\usage{
set_rounding(data, variables, digits, method = "decimal")
}
\arguments{
\item{data}{A \code{bq_data} object.}

\item{variables}{A tidyselect expression selecting one or more columns.}

\item{digits}{Number of decimal places or significant digits. Decimal places
may be zero; significant digits must be at least one.}

\item{method}{Either \code{"decimal"} or \code{"significant"}.}
}
\value{
\code{data} with updated rounding metadata.
}
\description{
Records how numeric estimates for one or more variables should eventually
be presented. \code{"decimal"} means a fixed number of digits after the decimal
point; \code{"significant"} means a number of significant digits. Stored values
and analytic results are never rounded by this setter.
}
\details{
The policy is value-dependent metadata and is cleared if a column is later
overwritten. Formatting of counts, p-values and other dimensionless output
belongs to their own result components rather than to this variable-level
policy.
}
\examples{
data <- as_bq_data(data.frame(age = c(40, 55), marker = c(0.0123, 0.0456)))
data <- set_rounding(data, age, digits = 0)
set_rounding(data, marker, digits = 3, method = "significant")
}
