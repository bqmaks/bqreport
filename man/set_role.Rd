% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bq-data.R
\name{set_role}
\alias{set_role}
\title{Add an analytical role to variables}
\usage{
set_role(.data, .cols, role)
}
\arguments{
\item{.data}{A \code{bq_data} object.}

\item{.cols}{Columns selected using tidyselect syntax.}

\item{role}{A single supported role.}
}
\value{
\code{.data} with an updated variable registry.
}
\description{
Roles are additive: a variable can simultaneously be, for example, a
\code{group} and a \code{predictor}. Assigning the same role repeatedly has no effect.
}
\examples{
data <- as_bq_data(tibble::tibble(
  patient_id = c("p1", "p2", "p3"),
  arm = factor(c("A", "B", "A"))
)) |>
  set_role(patient_id, "id") |>
  set_role(arm, "group")
variables(data)$role
}
