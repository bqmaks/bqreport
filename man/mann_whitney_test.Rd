% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mann-whitney-test.R
\name{mann_whitney_test}
\alias{mann_whitney_test}
\title{Declare a Mann-Whitney test}
\usage{
mann_whitney_test(
  exact = "auto",
  continuity_correction = TRUE,
  hypothesis = "two_sided",
  margin = NULL,
  benefit = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{exact}{Exactness policy. Use \code{"auto"} to choose explicitly from the
observed sample sizes and ties before calling \code{\link[stats:wilcox.test]{stats::wilcox.test()}},
\code{TRUE} to require an exact test or \code{FALSE} to require an asymptotic test.}

\item{continuity_correction}{Whether to apply the continuity correction for
an asymptotic test.}

\item{hypothesis}{Hypothesis type. One of \code{"two_sided"}, \code{"equivalence"},
\code{"noninferiority"} and \code{"superiority"}.}

\item{margin}{Hypothesis margin on the Hodges-Lehmann location-shift scale.
The rules are the same as for \code{\link[=t_test]{t_test()}}.}

\item{benefit}{Which outcome direction is beneficial for noninferiority and
superiority: \code{"higher"} or \code{"lower"}.}

\item{conf_level}{Confidence level for the Hodges-Lehmann location-shift
interval. Must be one finite number strictly between zero and one.}
}
\value{
A \code{bq_mann_whitney_test} analytic function.
}
\description{
Creates an analytic function for an independent two-group Mann-Whitney test
with its exactness policy, continuity correction and confidence level fixed
before analysis. The returned function is executed later by an analysis
plan with data and a compiled analysis context.
}
\details{
The analytic function supplies only the test result available from
\code{\link[stats:wilcox.test]{stats::wilcox.test()}}. It does not return a fitted model, a separate effect
size or within-group estimates.
}
\examples{
analysis <- mann_whitney_test(
  exact = "auto",
  continuity_correction = TRUE,
  conf_level = 0.95
)
analysis
}
