% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable-levels.R
\name{variable_levels}
\alias{variable_levels}
\title{Inspect ordered variable levels}
\usage{
variable_levels(data)
}
\arguments{
\item{data}{A \code{bq_data} object.}
}
\value{
A plain tibble with columns \code{var_id}, \code{value} and \code{position}.
}
\description{
Returns the flat registry that stores declared category order. Each row is
one level of one variable; \code{var_id} links it to \code{\link[=variables]{variables()}}, and \code{position}
records the order independently of the current row arrangement.
}
\examples{
data <- as_bq_data(data.frame(
  severity = c("low", "high")
))
data <- set_type(data, severity, ordinal(c("low", "medium", "high")))
variable_levels(data)
}
